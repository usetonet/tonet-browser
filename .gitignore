# Artefactos de compilación de Rust
/target
# Restos del prototipo retirado `crates/vela` (carpeta local / target embebido)
/crates/vela/

# Paquetes de instalación generados localmente o en CI
/dist

# Landing (Node / Vite / Wrangler / Astro)
web/landing/node_modules
web/landing/dist
web/landing/.wrangler
web/landing/.astro

# Entorno local (opcional)
.env
.env.*

# Salida local de scripts/cdn (merge prev + version.json antes de subir a R2)
/prev.json
/version.json

# Generated for local Vite dev (npm run dev)
web/landing/public/version.json
