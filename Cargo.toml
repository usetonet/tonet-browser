# Workspace Tonet: escritorio (`crates/tonet`) + web (`web/landing`) por separado.
[workspace]
members = ["crates/tonet"]
resolver = "2"
default-members = ["crates/tonet"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
