{
  "name": "tonet-landing",
  "private": true,
  "type": "module",
  "scripts": {
    "gen:manifest": "node scripts/sync-public-manifest.mjs",
    "dev": "npm run gen:manifest && vite",
    "build": "vite build && node scripts/embed-version.mjs",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-icons": "^5.4.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "~5.7.0",
    "vite": "^6.0.0",
    "wrangler": "^3.99.0"
  }
}
